

const getDeviceManager = () => {

  const devices = { };

  const addDevice = () => {

  };
  const removeDevice = () => {

  };
  const listDevices = () => {

  };

  const deviceManager = {
    addDevice,
    removeDevice,
    listDevices,
  };

  return deviceManager;
};

module.exports = getDeviceManager;